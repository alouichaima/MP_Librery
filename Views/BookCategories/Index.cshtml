@model IEnumerable<Librery_ProjectF.Models.BookCategory>

@{
    ViewData["Title"] = "Index";
}


<p><br />
    <a asp-action="Create" class="btn btn-primary">Create </a>
</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BookCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BookCategoryName)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.BookCategoryId"><img src="~/images/modifier.png" width="22px" height="22px" /></a> |
                    <a asp-action="Details" asp-route-id="@item.BookCategoryId"><img src="~/images/details.png" width="22px" height="22px" /></a> |
                    <a asp-action="Delete" asp-route-id="@item.BookCategoryId"><img src="~/images/supprimer.png" width="22px" height="22px" /></a>
            </td>
        </tr>
}
    </tbody>
</table>
